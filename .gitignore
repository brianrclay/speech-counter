.DS_Store
node_modules/
.vscode/

# Regenerated by `npm run build` from index.html/css/main.js/assets -
# Capacitor's own native-project gitignores already exclude the copies
# it makes of this into ios/ and android/.
www/
